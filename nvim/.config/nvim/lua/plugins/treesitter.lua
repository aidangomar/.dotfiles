local M = {
    "nvim-treesitter/nvim-treesitter",
    -- build = function()
    --     require("nvim-treesitter.install").setup({ 
    --       highlight = {
    --         disable = { "html" }
    --       },
    --       indent = {
    --         disable = "python"
    --       }
    --     })
    --       .update({ with_sync = true })()
    -- end,
}

return { M }


